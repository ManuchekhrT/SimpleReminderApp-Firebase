<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground">

    <include layout="@layout/toolbar" />


    <EditText
        android:id="@+id/reminderBodyEdt"
        android:layout_width="match_parent"
        android:layout_height="191dp"
        android:layout_margin="@dimen/text_margin"
        android:background="@drawable/edit_style"
        android:fontFamily="@font/montserrat_italic"
        android:gravity="top"
        android:hint="@string/label_reminder_text"
        android:textColor="@android:color/white"
        android:paddingStart="15dp"
        android:paddingLeft="15dp"
        android:paddingTop="15dp"
        android:textStyle="italic"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/mainToolbar"
        tools:ignore="RtlSymmetry" />

    <Button
        android:id="@+id/navigateToDateViewBtn"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:background="@drawable/btn_style"
        android:fontFamily="@font/montserrat_bold"
        android:text="@string/label_next"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="15sp"
        app:layout_constraintEnd_toEndOf="@id/reminderBodyEdt"
        app:layout_constraintStart_toStartOf="@id/reminderBodyEdt"
        app:layout_constraintTop_toBottomOf="@id/reminderBodyEdt" />


</androidx.constraintlayout.widget.ConstraintLayout>